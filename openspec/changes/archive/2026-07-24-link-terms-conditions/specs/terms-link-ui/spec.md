## ADDED Requirements

None — this change is a pure UI fix with no new functional requirements. Existing checkbox behavior (validation, state, submission) is unchanged.

## MODIFIED Requirements

None — no existing requirements changed.

## REMOVED Requirements

None

## 1. Create ResetLink component

- [x] 1.1 Create `src/components/atoms/ResetLink.tsx` — a React island that renders a muted "Start over" text link, shows `window.confirm()`, calls `useFormStore.reset()`, and redirects to `/step1` on confirmation

## 2. Add ResetLink to StepLayout

- [x] 2.1 Import and render `ResetLink` in `src/layouts/StepLayout.astro` below the navigation footer row
